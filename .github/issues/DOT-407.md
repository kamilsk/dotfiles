---
code: DOT-407
id: 416
database_id: 1371193321
node_id: I_kwDOCTsueM5Rur_p
status: open
title: "git: alias: unified abort command"
labels: ["type: feature","scope: code","impact: medium","effort: medium"]
url: https://github.com/kamilsk/dotfiles/issues/416
created_at: 2022-09-13T10:06:18Z
updated_at: 2026-09-17T11:09:49Z
---

# git: alias: unified abort command

Add a `git abort` that runs whichever `--abort` applies to the repository's state:

```bash
$ git abort
# instead of
# git merge --abort
# git rebase --abort
# git cherry-pick --abort
```

**Motivation:** developer experience — the operation in progress is visible in `.git` (`MERGE_HEAD`, `rebase-merge/` or `rebase-apply/`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`), so the alias can dispatch without the user remembering which command started it. The twin `git continue` was closed unbuilt in [#61](DOT-60.md).
