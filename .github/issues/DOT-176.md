---
code: DOT-176
id: 177
database_id: 946787466
node_id: MDU6SXNzdWU5NDY3ODc0NjY=
status: closed
title: "define Shell Script Libraries"
labels: []
url: https://github.com/kamilsk/dotfiles/issues/177
created_at: 2021-07-17T10:33:05Z
updated_at: 2021-10-30T19:38:20Z
---

# define Shell Script Libraries

Organise the shell code as libraries in the sense of [BashSupport Pro's "libraries"](https://www.bashsupport.com/manual/navigation/libraries/): directories of sourceable files that the IDE (and the reader) can resolve `source` statements against, so that functions have a home file, navigation works, and the rc files only load libraries.

<!-- 2026-09-16T05:49Z https://github.com/kamilsk/dotfiles/issues/177#issuecomment-5711685720
Code vs intent: the aggregate files have drifted from their sources. `bin/lib/git.bash` has `git-amend … --no-edit ${1+"${@}"}` (safe under `set -u`), while `bin/lib/git/commit.bash` has `"${@}"`; `bin/lib/sys.bash` differs from `sys/clone.bash` in formatting; `git/whoami.bash` and `sys/sign-in.bash` are not included at all. There is no script that regenerates the aggregates from the `#include` markers, so "the aggregate is the truth" in practice and the sources are documentation. `.github/reviews/20260709T130934Z.md` finding 12.
-->
