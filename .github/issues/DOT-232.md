---
code: DOT-232
id: I_kwDOCTsueM5A5SAL
databaseId: 1088757771
number: 233
url: https://github.com/kamilsk/dotfiles/issues/233
title: "refactor volumes and volume aliases"
labels: []
milestone:
state: OPEN
stateReason:
createdAt: 2021-12-26T12:04:46Z
updatedAt: 2026-09-17T11:02:06Z
lastEditedAt: 2026-09-17T11:02:06Z
closedAt:
---

# refactor volumes and volume aliases

Reshape the Docker volume helpers ([[DOT-13]]) into one command with subcommands:

```
$ volumes -> volume
$ volume ls
$ volume ls ...id
$ volume attach ...id
$ volume clean
$ volume rm
```

Today `volumes [all|clean]` lists or prunes and `volume <name>` mounts a volume into an `alpine` shell; the proposal merges them under `volume` with explicit verbs (`ls` with optional ids, `attach`, `clean`, `rm`). The second note is unrelated to Docker: trim Oh My Zsh's `grep` alias from `grep --color=auto --exclude-dir={.bzr,CVS,.git,.hg,.svn,.idea,.tox}` to `grep --color=auto`, i.e. stop the `common-aliases` plugin from hiding results in `.idea`/`.git`.

<!-- 2022-01-15T06:04Z https://github.com/kamilsk/dotfiles/issues/233#issuecomment-1013620744
also, output type, e.g. docker volume
-->
